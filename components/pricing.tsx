import HomePricingSection from "./home-pricing-section";

/** Legacy alias — use components/home-pricing-section.tsx directly. */
export default function Pricing() {
  return <HomePricingSection />;
}
